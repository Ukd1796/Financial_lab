[build]
builder = "nixpacks"

[deploy]
startCommand = "python run_signals.py"
cronSchedule = "5 10 * * 1-5"
# run-orders service: override startCommand to "python run_orders.py"
#                     and cronSchedule to "45 3 * * 1-5" in Railway dashboard
